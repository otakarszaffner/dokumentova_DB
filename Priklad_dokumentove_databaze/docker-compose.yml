version: "3.9"
services:
  couchdb:
    image: couchdb:latest 
    ports:
      - "5984:5984"  
    volumes:
      - couchdb_data:/opt/couchdb/data # Persistentní uložení dat
    environment:
      - COUCHDB_USER=admin  
      - COUCHDB_PASSWORD=password 

volumes:
  couchdb_data: